# URL Shortener

Сервис для сокращения URL-адресов, написанный на Go.

## 🚀 Возможности

- ✅ Сокращение длинных URL
- ✅ Пользовательские alias для ссылок
- ✅ Автоматическая генерация alias
- ✅ Обработка коллизий при генерации
- ✅ Basic Auth аутентификация
- ✅ SQLite база данных
- ✅ Функциональные тесты
- ✅ Middleware для логирования

## 🛠 Технологии

- **Go 1.21+**
- **SQLite** - база данных
- **Chi Router** - HTTP роутер
- **slog** - структурированное логирование
- **httpexpect** - функциональное тестирование

## 📦 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/Maslyukov-A-M-22-IVT-4/url-shortener.git
cd url-shortener
```

### 2. Установка зависимостей
```bash
go mod download
```

### 3. Настройка конфигурации
Создайте файл `.env`:
```env
CONFIG_PATH=./config/local.yaml
CGO_ENABLED=1
```

### 4. Запуск сервера
```bash
go run cmd/url-shortener/main.go
```

Сервер будет доступен по адресу: `http://localhost:8082`

## 🔧 API

### Создание короткой ссылки
```bash
POST /url
Authorization: Basic myuser:mypass
Content-Type: application/json

{
  "url": "https://example.com",
  "alias": "my-link"  // опционально
}
```

**Ответ:**
```json
{
  "status": "OK",
  "alias": "my-link"
}
```

### Переход по короткой ссылке
```bash
GET /{alias}
```

Возвращает HTTP 302 редирект на оригинальный URL.

## 🧪 Тестирование

### Запуск unit тестов
```bash
go test ./internal/...
```

### Запуск функциональных тестов
```bash
# Сначала запустите сервер
go run cmd/url-shortener/main.go

# В другом терминале запустите тесты
go test ./tests/... -v
```

## 📁 Структура проекта

```
url-shortener/
├── cmd/url-shortener/          # Точка входа приложения
├── internal/                   # Внутренняя логика
│   ├── config/                 # Конфигурация
│   ├── http-server/            # HTTP сервер и handlers
│   ├── lib/                    # Вспомогательные библиотеки
│   └── storage/                # Работа с базой данных
├── config/                     # Файлы конфигурации
├── tests/                      # Функциональные тесты
└── storage/                    # SQLite база данных
```

## 🔐 Аутентификация

По умолчанию используется Basic Auth:
- **Логин:** `myuser`
- **Пароль:** `mypass`

## 📝 Примеры использования

### Создание ссылки с пользовательским alias
```bash
curl -X POST http://localhost:8082/url \
  -H "Content-Type: application/json" \
  -H "Authorization: Basic bXl1c2VyOm15cGFzcw==" \
  -d '{"url": "https://github.com", "alias": "github"}'
```

### Создание ссылки с автогенерацией alias
```bash
curl -X POST http://localhost:8082/url \
  -H "Content-Type: application/json" \
  -H "Authorization: Basic bXl1c2VyOm15cGFzcw==" \
  -d '{"url": "https://google.com"}'
```

### Переход по ссылке
```bash
curl -L http://localhost:8082/github
```

## 🏗 Архитектура

Проект следует принципам **Clean Architecture** с разделением на слои:

- **Handlers** - HTTP обработчики
- **Use Cases** - бизнес-логика
- **Storage** - работа с данными
- **Config** - конфигурация приложения

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👨‍💻 Автор

**Маслюков А.М.** - [GitHub](https://github.com/Maslyukov-A-M-22-IVT-4)